import styled from 'styled-components';

import Button from 'components/Button';

export const TextLoginError = styled.div`
  font-size: 20px;
  text-align: center;
`;

export const ErrorButtonRegister = styled(Button)`
  margin-top: 8px;
  padding: 16px 40px;
`;
