import styled from 'styled-components';

import Button from 'components/Button';

export const TextLoginError = styled.div`
  font-size: 20px;
  text-align: center;
`;

export const StyledButton = styled(Button)`
  flex: 1;
`;

export const ErrorButton = styled(Button)`
  margin-top: 8px;
  padding: 16px 40px;
`;
